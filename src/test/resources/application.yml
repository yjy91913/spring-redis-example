#服务启动端口
server :  
  port :  8081

#数据库配置
spring:
    redis:
      host: 192.168.2.5
      #redis密码，没有密码的可以用～表示
      password: ~
      port: 6381
      pool:
        max-active: 100
        max-idle: 10
        max-wait: 100000

    
